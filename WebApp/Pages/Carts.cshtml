@page
@model WebApp.Pages.CartsModel
@{
}

<div class="my-container mx-auto border border-dark">
    <div class="p-4 border my-rounded-corner">
        <h3 class="w-25 border text-center py-2 my-rounded-corner">Current Cart</h3>
        <div class="mt-5">
            <div class="d-grid w-50 mx-auto mb-3">
                <button class="btn btn-outline-success fs-4" type="button" asp-page="/Orders">Order</button>
            </div>

            <table class="table w-50 mx-auto table-bordered table-hover myshadow">
                <tbody>
                    <tr>
                        <td>Total Items</td>
                        @*<td class="fs-5 fw-bold">@usersCart.CartItems.Count</td>*@
                    </tr>
                    @*<tr>
                        <td>Total Price</td>
                        <td class="fs-5 fw-bold text-danger">@usersPaymentInfo.TotalAmount</td>
                    </tr>
                    <tr>
                        <td>Shipping Charges</td>
                        <td class="fs-5 fw-bold text-danger">@usersPaymentInfo.ShippingCharges</td>
                    </tr>
                    <tr>
                        <td>Discount</td>
                        <td class="fs-5 fw-bold text-success">@usersPaymentInfo.AmountReduced</td>
                    </tr>
                    <tr>
                        <td>You have to Pay</td>
                        <td class="fs-5 fw-bold text-success">@usersPaymentInfo.AmountPaid</td>
                    </tr>*@
                </tbody>
            </table>
        </div>

        <div class="current-cart-items w-75 mx-auto mt-5">
            <h4>Cart Items</h4>
            <div class="row mt-4 g-0">
                @*@foreach (var cartItem in usersCart.CartItems)
                {
                    <div class="col-6 py-3 px-2">
                        <partial name="~/Views/Shared/_Products.cshtml" model="cartItem.Product" view="currcartitem" />
                    </div>
                }*@
            </div>
        </div>
    </div>

    @*<div class="mt-5 p-4 border my-rounded-corner">
        <h3 class="w-25 border text-center py-2 mb-5 my-rounded-corner">Previous Cart</h3>
        @foreach (var cart in usersPreviousCarts)
        {
            <div class="card mb-5">
                <div class="card-header d-flex fw-bold justify-content-between align-items-center">
                    <span class="time">Order On: <span class="fs-5">@cart.OrderedOn.ToString("dd-MMM-YYYY")</span></span>
                    <span class="count">Item: @cart.CartItems.Count</span>
                    <span class="price">Price Paid: <span class="fs-5">@utilityService.CalculatePricePaid(cart)</span></span>
                </div>
                <div class="cart-body px-4 py-2">
                    <div class="row g-0">
                        @foreach (var cartItem in cart.CartItems)
                        {
                            <div class="col-2 px-2 py-2">
                                <partial name="~/Views/Shared/_Products.cshtml" model="cartItem.Product" view="prevcartitem" />
                            </div>
                        }
                    </div>
                </div>
            </div>
        }
    </div>*@
</div>

